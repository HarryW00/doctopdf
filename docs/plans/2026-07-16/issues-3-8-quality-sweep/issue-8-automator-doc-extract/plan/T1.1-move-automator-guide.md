# Worker Prompt: T1.1-move-automator-guide

- **Source task**: Batch 1 / Task 1 — Move the Automator Quick Action guide to `docs/automator.md`

---

## 1. Mission & Rules

### Mission

Move the full "Automator Quick Action (Finder Right-Click)" section from `README.md` **verbatim** into a new `docs/automator.md`, and replace the README section with a short summary + a link to that file. No content is lost and no instructional text is edited.

### Rules

- Modify ONLY `README.md` and create `docs/automator.md`.
- **Verbatim move** — copy the section's content exactly (including code blocks); do not rewrite steps.
- Fix any **relative image links** so they still resolve from `docs/automator.md` (e.g. `docs/images/x.png` referenced from README becomes `../docs/images/x.png` from `docs/`, or keep absolute repo-relative paths if already rooted).
- If the section boundary is ambiguous, report it — do not guess.
- Workers are leaf nodes — do not spawn sub-workers.

---

## 2. Context

### Input Files

- `README.md` — the section to move begins at the heading `## Automator Quick Action (Finder Right-Click)` and ends immediately before the next `## ` (level-2) heading.
- `references/` — none needed.

### Background

The Automator section is ~60 lines and targets an advanced use case; relocating it keeps the README focused on the terminal workflow (SPEC #8, Req 1–2). `cli.py`'s epilog still says "See README.md for Finder right-click integration" — leave it; the README will link onward, so it stays correct.

---

## 3. Tasks

### `docs/automator.md` — create

1. Create `docs/` (if absent) and `docs/automator.md`.
2. Copy the entire README "Automator Quick Action (Finder Right-Click)" section (from its `## ` heading through the line before the next `## ` heading) into `docs/automator.md`, verbatim.
3. Adjust any relative image paths in that content so they resolve from `docs/` (add `../` prefix to repo-relative links as needed).

### `README.md` — replace the section with a summary + link

1. Delete the full Automator section (the same span moved above).
2. In its place, insert:
   ```markdown
   ## Automator Quick Action (Finder Right-Click)

   You can create a macOS Finder right-click service to convert documents without opening Terminal.

   → **[Full setup guide: docs/automator.md](docs/automator.md)**
   ```

### Output

Report: **Files modified/created** (`README.md`, `docs/automator.md`), the **line span moved**, **image-path adjustments made** (or "none"), **risks** (or "None").

---

## 4. Verification

1. Run: `test -f docs/automator.md && echo ok` → prints `ok`.
2. Run: `grep -c "Full setup guide: docs/automator.md" README.md` → `1`.
3. Run: `grep -c "## Automator Quick Action (Finder Right-Click)" README.md` → `1` (the summary heading remains).
4. Manual: confirm the full step-by-step content now lives in `docs/automator.md` and no setup steps were dropped or edited.

---

## 5. Scope & References

### Allowed Files
- `README.md` — replace the Automator section with a summary + link.
- `docs/automator.md` — create with the verbatim moved content.

### Forbidden Files
- `doctopdf/**`, `pyproject.toml`, `.github/**` — not touched by #8.
- `cli.py` — epilog intentionally left unchanged.

### Related References
- `issue-8-automator-doc-extract/SPEC.md`, `issue-8-automator-doc-extract/DESIGN.md`, `issue-8-automator-doc-extract/CHECKLIST.md`.

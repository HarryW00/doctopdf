# Worker Prompt: T1.4-dev-extra-extend

- **Source task**: Batch 1 / Task 4 — Extend the `dev` extra in `pyproject.toml`

---

## 1. Mission & Rules

### Mission

Extend the `[project.optional-dependencies] dev` group in `pyproject.toml` (bootstrapped to `["pytest"]` by #6) to also include `pytest-cov`, `ruff`, `mypy`.

### Rules

- Modify ONLY `pyproject.toml`, and ONLY the `dev` list under `[project.optional-dependencies]`.
- Do not add version pins (float) unless a pin already exists for `pytest`.
- If the `dev = ["pytest"]` line is absent (i.e., #6 did not run), STOP and report — do not create it.
- Workers are leaf nodes.

---

## 2. Context

### Input Files

- `pyproject.toml` — contains `[project.optional-dependencies]` with `dev = ["pytest"]` (added by #6).

### Background

#4 needs `pytest-cov` (coverage reporting), and #7 needs `ruff` + `mypy` available via the same `dev` extra so CI can install everything with `pip install -e ".[dev]"`.

---

## 3. Tasks

### `pyproject.toml` — extend the `dev` extra

1. Open `pyproject.toml`.
2. Find under `[project.optional-dependencies]`:
   ```toml
   dev = ["pytest"]
   ```
3. Replace with:
   ```toml
   dev = ["pytest", "pytest-cov", "ruff", "mypy"]
   ```

### Output

Report: **Files modified** (`pyproject.toml`), the before/after line, **risks** (or "None").

---

## 4. Verification

1. Run: `pip install -e ".[dev]"` → succeeds (installs pytest, pytest-cov, ruff, mypy).
2. Run: `python -c "import pytest, pytest_cov, ruff" 2>/dev/null; mypy --version` → both succeed (no import error; mypy prints a version).

---

## 5. Scope & References

### Allowed Files
- `pyproject.toml` — only the `dev` list under `[project.optional-dependencies]`.

### Forbidden Files
- `doctopdf/**`, `tests/**`, `.github/**` — owned by other workers / other issues.

### Related References
- `issue-4-unit-test-suite/SPEC.md`, `issue-4-unit-test-suite/DESIGN.md` (§10 hand-off note).

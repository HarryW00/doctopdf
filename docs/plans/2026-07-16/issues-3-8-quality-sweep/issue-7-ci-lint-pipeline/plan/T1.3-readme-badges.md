# Worker Prompt: T1.3-readme-badges

- **Source task**: Batch 1 / Task 3 — Update README badges (Python 3.11+ and CI badge)

---

## 1. Mission & Rules

### Mission

In `README.md`: update the "Python 3.9+" badge to "Python 3.11+" and add a CI badge beneath the existing badge row.

### Rules
- Modify ONLY `README.md`, and ONLY badge lines.
- Preserve all other content. Do not touch the badges for Platform, Automation, License, or Version.
- If the badge line cannot be found exactly, report it — do not guess.
- Workers are leaf nodes.

---

## 2. Context

### Input Files
- `README.md` — top badge block (around lines 5–9):
  - `![Python 3.9+](https://img.shields.io/badge/Python-3.9+-blue?logo=python&logoColor=white)`
  - … Platform / Automation / License / Version badges follow …

### Background
With `requires-python` bumped to 3.11 (T1.2), the Python badge must reflect 3.11+. A CI badge links the README to the new `lint.yml` workflow (SPEC #7 Req 3).

---

## 3. Tasks

### `README.md` — update the Python badge

1. Find:
   ```markdown
   ![Python 3.9+](https://img.shields.io/badge/Python-3.9+-blue?logo=python&logoColor=white)
   ```
2. Replace with:
   ```markdown
   ![Python 3.11+](https://img.shields.io/badge/Python-3.11+-blue?logo=python&logoColor=white)
   ```

### `README.md` — add the CI badge

1. Immediately after the Version badge line:
   ```markdown
   ![Version: 1.1.0](https://img.shields.io/badge/version-1.1.0-brightgreen)
   ```
2. Insert this new line directly beneath it:
   ```markdown
   ![CI](https://github.com/HarryW00/doctopdf/actions/workflows/lint.yml/badge.svg)
   ```

### Output
Report: **Files modified** (`README.md`), the before/after, **risks** (or "None").

---

## 4. Verification

1. Run: `grep -c "Python-3.9+" README.md` → `0`.
2. Run: `grep -c "Python-3.11+" README.md` → `1`.
3. Run: `grep -c "actions/workflows/lint.yml/badge.svg" README.md` → `1`.

---

## 5. Scope & References

### Allowed Files
- `README.md` — badge lines only.

### Forbidden Files
- `pyproject.toml`, `.github/**`, `doctopdf/**`, `docs/**`.

### Related References
- `references/ci-tooling.md`; `issue-7-ci-lint-pipeline/SPEC.md` (Req 3).
